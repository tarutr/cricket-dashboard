# Pinned to the exact versions installed locally when this pipeline safety-net
# batch was validated (2026-07-09) -- see the pipeline-safety branch report for
# how each was verified against actual imports:
#   duckdb    -- export_parquet.py, pipeline/build_profiles.py, pipeline/ingest.py
#   boto3     -- pipeline/download_db.py, pipeline/upload_db.py, export_parquet.py (_r2_client)
#   botocore  -- pipeline/state_store.py (direct `import botocore` for exception types)
#   requests  -- pipeline/sheet_fetch.py, pipeline/ingest.py
#   tqdm      -- pipeline/ingest.py
#   numpy     -- NOT imported anywhere, but REQUIRED at runtime by duckdb's
#                con.create_function() (Python UDFs, used in build_profiles.py's
#                norm_alnum). Removing it as "unused" broke the 2026-07-10 CI run
#                with: "'numpy' is required for this operation". Do not remove.
duckdb==1.5.3
boto3==1.43.27
botocore==1.43.27
requests==2.34.2
tqdm==4.68.2
numpy==2.4.6
